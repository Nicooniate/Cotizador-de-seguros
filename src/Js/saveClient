import { newClient } from './app'

const fs = require('fs');

function saveClient(){
    const newClientJSON = JSON.stringify(newClient)
    fs.writeFile('clientes.json', newClientJSON, (error)=>{
        if(error) throw error;
        console.log("Cliente guardado en base de datos")
    })
}
